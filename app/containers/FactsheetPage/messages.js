/*
 * FactsheetPage Messages
 *
 * This contains all the text for the FactsheetPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FactsheetPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FactsheetPage container!',
  },
});
